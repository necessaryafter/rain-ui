{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Rain UI Contract Schema v0",
  "description": "JSON Schema for Rain UI screen contracts (schemaVersion 0)",
  "type": "object",
  "required": [
    "schemaVersion",
    "id",
    "properties",
    "actions",
    "root"
  ],
  "properties": {
    "schemaVersion": {
      "type": "integer",
      "const": 0,
      "description": "Contract schema version — only 0 is supported in M1"
    },
    "id": {
      "type": "string",
      "pattern": "^[a-z0-9_-]+:[a-z0-9_/-]+$",
      "description": "Screen identifier in Minecraft namespace format (namespace:path)"
    },
    "properties": {
      "type": "object",
      "additionalProperties": {
        "$ref": "#/definitions/TypeSchema"
      },
      "description": "Root-level data properties the server sends to this screen"
    },
    "actions": {
      "type": "object",
      "additionalProperties": {
        "$ref": "#/definitions/TypeSchema"
      },
      "maxProperties": 256,
      "description": "Actions this screen can dispatch, mapped to payload schemas"
    },
    "root": {
      "$ref": "#/definitions/ComponentNode",
      "description": "Root component node of the screen's layout tree"
    },
    "assets": {
      "type": "object",
      "maxProperties": 256,
      "propertyNames": {
        "pattern": "^[0-9a-f]{64}$"
      },
      "additionalProperties": {
        "$ref": "#/definitions/AssetInfo"
      },
      "description": "Assets the screen can download, keyed by the lowercase hex sha256 of their bytes; absent when there are none"
    },
    "assetNames": {
      "type": "object",
      "additionalProperties": {
        "type": "string",
        "pattern": "^[0-9a-f]{64}$"
      },
      "description": "Names a t.asset() property can carry, mapped to hashes in assets; absent when there are none"
    }
  },
  "definitions": {
    "TypeSchema": {
      "oneOf": [
        {
          "$ref": "#/definitions/TypeString"
        },
        {
          "$ref": "#/definitions/TypeInt"
        },
        {
          "$ref": "#/definitions/TypeLong"
        },
        {
          "$ref": "#/definitions/TypeDouble"
        },
        {
          "$ref": "#/definitions/TypeBool"
        },
        {
          "$ref": "#/definitions/TypeItem"
        },
        {
          "$ref": "#/definitions/TypeAsset"
        },
        {
          "$ref": "#/definitions/TypeList"
        },
        {
          "$ref": "#/definitions/TypeObject"
        }
      ],
      "description": "Type schema for properties, actions, and object fields"
    },
    "TypeString": {
      "type": "object",
      "required": [
        "kind"
      ],
      "properties": {
        "kind": {
          "const": "string"
        },
        "optional": {
          "const": true,
          "description": "The value may be absent or null"
        },
        "default": {
          "type": "string",
          "maxLength": 1024,
          "description": "Used when the value is absent or null; implies optional. Only allowed in properties"
        }
      },
      "additionalProperties": false
    },
    "TypeInt": {
      "type": "object",
      "required": [
        "kind"
      ],
      "properties": {
        "kind": {
          "const": "int"
        },
        "optional": {
          "const": true,
          "description": "The value may be absent or null"
        },
        "default": {
          "type": "integer",
          "description": "Used when the value is absent or null; implies optional. Only allowed in properties"
        }
      },
      "additionalProperties": false
    },
    "TypeLong": {
      "type": "object",
      "required": [
        "kind"
      ],
      "properties": {
        "kind": {
          "const": "long"
        },
        "optional": {
          "const": true,
          "description": "The value may be absent or null"
        },
        "default": {
          "type": "integer",
          "description": "Used when the value is absent or null; implies optional. Only allowed in properties"
        }
      },
      "additionalProperties": false
    },
    "TypeDouble": {
      "type": "object",
      "required": [
        "kind"
      ],
      "properties": {
        "kind": {
          "const": "double"
        },
        "optional": {
          "const": true,
          "description": "The value may be absent or null"
        },
        "default": {
          "type": "number",
          "description": "Used when the value is absent or null; implies optional. Only allowed in properties"
        }
      },
      "additionalProperties": false
    },
    "TypeBool": {
      "type": "object",
      "required": [
        "kind"
      ],
      "properties": {
        "kind": {
          "const": "bool"
        },
        "optional": {
          "const": true,
          "description": "The value may be absent or null"
        },
        "default": {
          "type": "boolean",
          "description": "Used when the value is absent or null; implies optional. Only allowed in properties"
        }
      },
      "additionalProperties": false
    },
    "TypeItem": {
      "type": "object",
      "required": [
        "kind"
      ],
      "properties": {
        "kind": {
          "const": "item"
        },
        "optional": {
          "const": true,
          "description": "The value may be absent or null"
        }
      },
      "additionalProperties": false
    },
    "TypeAsset": {
      "type": "object",
      "required": [
        "kind"
      ],
      "properties": {
        "kind": {
          "const": "asset"
        },
        "optional": {
          "const": true,
          "description": "The value may be absent or null"
        }
      },
      "additionalProperties": false,
      "description": "The name of one of the screen's assets (see assetNames), never a hash or a URL"
    },
    "TypeList": {
      "type": "object",
      "required": [
        "kind",
        "of"
      ],
      "properties": {
        "kind": {
          "const": "list"
        },
        "of": {
          "$ref": "#/definitions/TypeSchema"
        },
        "optional": {
          "const": true,
          "description": "The value may be absent or null"
        }
      },
      "additionalProperties": false
    },
    "TypeObject": {
      "type": "object",
      "required": [
        "kind",
        "fields"
      ],
      "properties": {
        "kind": {
          "const": "object"
        },
        "fields": {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/definitions/TypeSchema"
          }
        },
        "optional": {
          "const": true,
          "description": "The value may be absent or null"
        }
      },
      "additionalProperties": false
    },
    "ComponentNode": {
      "type": "object",
      "required": [
        "type",
        "props",
        "children"
      ],
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "column",
            "row",
            "text",
            "item",
            "image",
            "button",
            "list",
            "show",
            "match",
            "case",
            "default",
            "fallback"
          ],
          "description": "Component type"
        },
        "props": {
          "type": "object",
          "description": "Component properties (structure varies by type)"
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/ComponentNode"
          },
          "description": "Child component nodes"
        }
      },
      "additionalProperties": false
    },
    "AssetInfo": {
      "type": "object",
      "required": [
        "type",
        "bytes"
      ],
      "properties": {
        "type": {
          "enum": [
            "image/png",
            "image/jpeg",
            "image/gif",
            "font/ttf",
            "font/otf"
          ]
        },
        "bytes": {
          "type": "integer",
          "minimum": 0,
          "maximum": 8388608
        },
        "width": {
          "type": "integer",
          "minimum": 1,
          "maximum": 4096
        },
        "height": {
          "type": "integer",
          "minimum": 1,
          "maximum": 4096
        },
        "frames": {
          "type": "integer",
          "minimum": 1,
          "maximum": 512
        }
      },
      "additionalProperties": false,
      "description": "An asset table entry; images carry width and height, GIFs also frames"
    }
  }
}
