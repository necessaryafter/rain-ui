package com.rainframework.ui.client.render;

/** How a box distributes the space left on its main axis when no child fills it. */
public enum Justify {
    START,
    CENTER,
    END,
    SPACE_BETWEEN
}
